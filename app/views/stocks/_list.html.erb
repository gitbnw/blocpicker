 <% if stocks.empty?  %>
   <h4 class="empty_head">Portfolio is empty.  Please add some stocks.</h4>
  <% else %>

  <div class="row" id="list_head">
   <div class="col-md-1 text-info">Symbol</div>
   <div class="col-md-1 text-info">Price</div>
   <div class="col-md-1 text-info">Change</div>
   <div class="col-md-1 text-info">Percent</div>
   <div class="col-md-2 text-info">Volume</div>
   <div class="col-md-2 text-info">Avg Dly Vol</div>
   <div class="col-md-1 text-info tag-tooltip" data-toggle="tooltip" data-placement="top" title="A fancy algorithm is telling you what to do.">Ticks</div>
  </div>
  
   <% stocks.each do |stock| %> 
<% raise %>
   <div class="js-stock row" id="<%= "stock-row-#{stock.id}" %>">
     <%= render stock %>
   </div>

  <% end %>
  
<script type="text/javascript">

  var refresh_rate = 60000

  init_count($("#portfolio_id").val(), refresh_rate);

</script>
 <% end %>
