<h2>Portfolios</h2>
<div class="row">
  <div class="col-md-6">
    <div class="media">
      <% if @user.present? %>
        <%= render @user.portfolios %>
      <% end %>
    </div>

  </div>
  <div class="col-md-2">

      <%= link_to "Add", new_portfolio_path(@portfolio), class: 'btn btn-success' %>

  </div>
